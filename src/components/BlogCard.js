import React, { Component } from 'react';
import { BlogCard } from '../styled-components/components/BlogCard';

class BlogCardComponent extends Component {
  render(){
    return (
      <BlogCard>
        Blog Card
      </BlogCard>
    );
  }
}

export default BlogCardComponent;
