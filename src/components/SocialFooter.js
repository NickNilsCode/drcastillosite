import React, { Component } from 'react';
import { SocialFooter } from '../styled-components/components/SocialFooter';

class SocialFooterComponent extends Component {
  render(){
    return (
      <SocialFooter>
        Social Footer
      </SocialFooter>
    );
  }
}

export default SocialFooterComponent;
