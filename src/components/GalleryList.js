import React, { Component } from 'react';
import { GalleryList } from '../styled-components/components/GalleryList';

class GalleryListComponent extends Component {
  render(){
    return (
      <GalleryList>
        Gallery List
      </GalleryList>
    );
  }
}

export default GalleryListComponent;
