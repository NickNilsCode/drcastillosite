import React, { Component } from 'react';
import { ConsultationForm } from '../styled-components/components/ConsultationForm';

class ConsultationFormComponent extends Component {
  render(){
    return (
      <ConsultationForm>
        Consultation Form
      </ConsultationForm>
    );
  }
}

export default ConsultationFormComponent;
